# 配置与密钥（含 api_key，绝不入仓）
backend/config.json

# Agent 记忆与角色头像（含真实业务经验 / 角色形象，属本地运行数据，不入公开仓）
memory/*.md
!memory/README.md
icon/

# 内部取数 / 知识库 skill（含内部接口地址与凭证、内部数据表名，绝不公开）
skills/narya-data/
skills/bilisc-kb-live-revenue/

# TestReport 内部测试脚本与运行产物（不入公开仓）
TestReport/

# SQLite 数据库（含备份，含真实数据/凭证，绝不入仓）
*.db
*.db.bak*
*.db.bak_*
*.db-journal
*.db-wal
*.bak
*.bak_*

# 记忆回滚备份（本地运行产物，不入仓）
backend/_mem_bak_*/
_mem_bak_*/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node / 前端
node_modules/
frontend/dist/
.vite/

# 日志与临时
*.log
.reimport_*.json
_tmp_*

# 环境与编辑器
.env
.env.local
.DS_Store
.idea/
.vscode/
