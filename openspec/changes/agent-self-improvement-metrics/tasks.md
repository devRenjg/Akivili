# Tasks

> 规划态清单。未打勾 = 未实现。分阶段推进，每阶段落实前再回来细化。

## L1 单 Agent 效率成长快照（近期目标）
- [ ] L1.1 定义「同类任务」的最小分型（依赖 L2.1；先用标题关键词粗分）
- [ ] L1.2 效率代理指标聚合：单任务耗时（task_runs 时间差之和）、返工次数（activities 里 reviewing→in_progress 回退计数）、求助次数（messages 反问 leader 轮次）、对话轮次
- [ ] L1.3 **knowhow 复用率（核心）**：定义「复用」= 引用级识别（后续任务产出中显性援引某条 knowhow）；产出复用率 + 复用分布 + 「僵尸经验」清单（沉淀但从未被援引）
- [ ] L1.4 输出只读「成长体检快照」（单 Agent × 任务类型）
- [ ] L1.5 明确标注局限：耗时可能是成本转移；复用率是显性援引非因果贡献；样本少时不拟合曲线只做粗对比
- [ ] L1.6 设计原则落地：本指标仅用于观测/人工诊断，**绝不接入 Agent 奖励**（防 Gaming）

## L2 任务分型 + 基线（L1 前置）
- [ ] L2.1 分型 = **角色 slug 作类型标签**（两级可延展：阶段A 角色即类型；阶段B 角色内再按关键词/痕迹分子类型）。不用纯标题关键词（真实数据会撞车）
- [ ] L2.2 **仪式任务过滤**：启发式（标题含 自我介绍/Kickoff）+ 执行痕迹兜底（desc=0 且无 task_runs 才算噪声；有 run 的空描述任务不误杀，如 task 64）
- [ ] L2.3 **单点先行**：按 Agent 逐个开启度量，任务数 ≥3 才出趋势，不足则标「样本不足暂不度量」（现状仅数据工程师够格）
- [ ] L2.4 每类型建「基线」，使纵向对比有意义

## L3 反事实探针（远期，最接近 SIP-Bench）
- [ ] L3.1 设计「老任务回归集」的选取与重跑机制
- [ ] L3.2 带/不带某条 knowhow 的对照重跑，测因果贡献
- [ ] L3.3 负迁移检测（论文重点警示：self-generated skill 会 negative transfer）

## 团队层（关联 agent-collaboration，更远期）
- [ ] T.1 团队协同效率趋势聚合（在单 Agent 层之上）
- [ ] T.2 ⚠️ 先解决论文 10.7 难点：区分「真协同收益」与「只是堆了算力」，否则团队指标会骗人
