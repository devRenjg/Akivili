# task-system

## MODIFIED Requirements

### Requirement: 子任务

任务 SHALL 可拆分子任务（带描述与优先级）并展示完成进度；子任务强制两级——子任务下不能再建子任务。看板 SHALL 在父任务卡片下嵌套展示其子任务。

#### Scenario: 子任务进度
- **WHEN** 任务有若干子任务
- **THEN** 展示 done/total 进度；子任务可独立改状态

#### Scenario: 子任务带描述与优先级
- **WHEN** 用户创建子任务
- **THEN** 系统保存其标题、描述、负责人与优先级

#### Scenario: 两级限制
- **WHEN** 用户试图在一个本身是子任务的任务下再建子任务
- **THEN** 系统拒绝（子任务下不能再创建子任务）

#### Scenario: 看板嵌套卡片
- **WHEN** 用户查看看板上的父任务卡片
- **THEN** 其下以小卡片嵌套展示各子任务（状态、优先级、负责人头像、标题），点击进入子任务详情

### Requirement: 两栏详情与执行日志

任务详情 SHALL 为两栏布局：主内容（描述+子任务+时间线+输入）与属性侧栏（状态/优先级/负责人/时间/执行日志）。执行日志区每次运行 SHALL 提供「日志详情」入口，查看该次执行的完整命令与运行时详情。

#### Scenario: 执行日志区
- **WHEN** 用户查看任务详情
- **THEN** 属性侧栏列出每次 Agent 运行（触发/状态/耗时），可展开查看该次执行的日志行

#### Scenario: 打开日志详情
- **WHEN** 用户点击某次运行的「日志详情」
- **THEN** 弹出结构化执行记录（含每条工具调用的完整命令与输出），支持过滤、排序、复制
