## Why

当前后端是「单进程」架构：HTTP/SSE、业务路由、多 Agent 协同调度（`_loop` 并发池）、CLI 子进程,全部绑在同一个 Python 进程里。因此**每次改代码/优化都要重启整个进程**,而重启会：

1. **中断正在跑的 Agent**——CLI 子进程是本进程用 `subprocess.Popen` 起的,进程一停,`_process_one` 协程消失、收尾逻辑跑不到,正在执行的 run 被判死(靠 `reclaim_orphan_runs` + 孤儿巡检补成 `killed`)。任务被迫重跑。
2. **断开用户连接**——用户看任务详情/看流式输出走的是本进程的 SSE 长连接,进程停即断,前端短暂报错。

用户诉求(已确认)：**改代码就重启,很频繁**,且要求**用户访问不断 + Agent 任务不中断**——即「无感知的平滑重启/热更新」。

**根因**：Agent 执行的寿命 = 后端进程寿命。要做到平滑重启,必须打破这个绑定——把「执行层」从「API 层」剥离,使 API 可以随意重启而 Agent 继续跑。

**路线修正**：早期方案设想「CLI 托孤」——让 CLI 子进程脱离 Worker 独立存活、Worker 重启后原子认领续跟(detached/文件唯一源/收尾重建/worker_id 认领/reclaim 反转/交棒标记)。评估后确认更优路线是**不做进程托孤,而走「温和重启 + 逻辑 session resume」**——重启时优先等在跑执行自然收尾（能等则零中断），等不到才中断、重启后靠持久化的 CLI `session_id` 把 run 重新入队、resume 续跑（best-effort 恢复，非「上下文必然不丢」）。一旦具备 resume 能力（见独立 change [agent-session-resume]），**托孤的进程级复杂度就不值得了**:温和重启→（超时）中断→resume 续接对多 Agent 平台已足够健壮,且实现量小一个数量级。故本 change 的 M2.5 改走「温和重启+resume」,砍掉全部托孤机制;M3 的 Nginx 蓝绿保留。

## What Changes

> **经技术负责人 Review 修订（2026-07-16，判定 Request changes）**：核心 = 先把方案从「resume 功能列表」升级为「持久化执行协议」——先定义 durable execution 状态机 + 并发不变量，再实现。分期改为**阶段 0-6（Worker 剥离优先、resume 后置）**。**本 change 暂不改任何代码。**

修正原方案 3 处事实错误：① `_claim_one` **非原子**（UPDATE 无 CAS，多 Worker 会双领）——原写「已原子/地基已具备」错误;② 人工 @ 主受理人**在 API 请求内同步执行**（`routes/runs.py`），M2 未覆盖此主路径;③ `run_queue` **无 conversation_id 列**，原 spec 写的 `(conversation_id, agent_slug)` partial index 建不出来——曾改用 `(task_id, agent_slug)` 保单 active，且「折叠」由丢弃改为合并。**（Review 第五轮 P1-5 拍板方案 B 已改回 conversation 粒度：迁移先给 `run_queue` 补 `conversation_id` 列再建 `(conversation_id, agent_slug)` partial unique index，与 session owner 键同粒度，防同 conversation 多 task 争抢 owner。以此为准。）**

- **阶段 0 — 补执行协议规格**：定义 execution 状态机 `queued→claimed→running→{done/failed/killed/superseded(+recovery child)/recovery_blocked}`（**execution 成功态=`done` 非 `succeeded`；`succeeded` 是 attempt 层终态**，双层状态词汇表见 spec，Review 第四轮 P0-1;**无独立 accepted 态**，Review P0-A）+ 7 条不变量（原子持有、单 active+单 pending intent、CAS 终态、supersede+child 同事务、旧 generation fencing、有界恢复、事务边界）。见 design 决策 0。
- **阶段 1 — DB 协议地基**：`WAL + 每连接 busy_timeout + 安全在线备份 + 索引`；**所有触发统一入队**；**两段式 dispatch**（POST 幂等入队返回 execution_id + GET 独立 SSE 订阅）；**原子 claim（CAS 单语句）**；重复触发合并（不丢）；`task_run↔run_queue` claim 时即建不可变关联。
- **阶段 2 — 执行层剥离**：独立 **Worker 进程**；API 不执行 Agent；Worker **supervisor/heartbeat/readiness**；**generation/lease/单实例**；CLI **进程 containment**（Windows Job Object / POSIX 进程组，防崩溃后孤儿 CLI）；kill 带 request/ack。**→ 重启 API 不断 Agent。**
- **阶段 3 — Claude resume**、**阶段 4 — Codex app-server**：归 [agent-session-resume]（省 token + 上下文连贯 + 为续跑提供 session）。
- **阶段 5 — 交棒 + 有界恢复**：restart generation + claim barrier；**defer 5 分钟**等自然收尾（能等则零中断）；超时 kill + **CAS supersede + recovery child 同事务**；**bounded recovery（次数上限/退避/dead-letter）**；stale generation fencing。**→ 改执行层温和重启、resume 续跑，副作用语义明确为 at-least-once。**
- **阶段 6 — 连接平滑**：**Nginx** 蓝绿；SSE **主动轮换 + Last-Event-ID** 自动重连（给旧 API 确定排空上限）；expand/contract 部署。验收口径 = 「自动重连、状态与日志不重不漏」，非绝对「同一连接不断」。

## Capabilities

### New Capabilities
- `platform-graceful-restart`: 平台的持久化执行协议 + 平滑重启能力——durable execution 状态机 + 并发不变量、原子 claim、两段式 dispatch、API/执行层解耦、进程 containment、Worker generation/fencing、温和重启（defer 优先）+ 超时 resume 续跑（有界恢复）、SSE 续传、反向代理蓝绿。综合达成:**改 API 层用户自动无损重连且 Agent 不中断;改执行层温和重启（能等则零中断，超时 resume 续跑，副作用 at-least-once）;无双执行、无触发丢失。** 恢复承诺按三层口径（平台消息 at-least-once / CLI 原生 session best-effort resume / 外部副作用非 exactly-once），**SHALL NOT 承诺「上下文必然不丢」或 exactly-once**（Review 第五轮 P1-7）。resume 能力由独立 change [agent-session-resume] 提供。

## Impact

- **规划态,暂不改代码。** 落实时预计涉及：`database.py`(WAL/busy_timeout/索引;`run_queue` 加列 conversation_id（第六轮 P1-1 conversation 粒度 active 索引所需）/idempotency_key/claim_owner/claim_generation/claimed_at/**claim_lease_until**（第六轮 P0-1，claimed 领取租约，区别于 `worker_state.lease_expires_at`）/current_attempt_id/final_attempt_id（第六轮 P1-3 指针，final 而非 winning）/recovery_chain_id/recovery_attempt/recovery_mode（session_resume|full_replay）/superseded_from（+ **`UNIQUE INDEX uq_run_queue_recovery_parent ON run_queue(superseded_from) WHERE superseded_from IS NOT NULL`** 硬约束防一父多 child，第十一轮 P0-B）/**migration_from_execution_id（第十五轮 P1-4，NULL conversation 迁移承接的普通 successor 因果键，+ `UNIQUE INDEX ON run_queue(migration_from_execution_id) WHERE migration_from_execution_id IS NOT NULL` 防重放建多个 successor，与 superseded_from 分离——迁移 successor 不挂恢复链）**/**continuation_from_execution_id + continuation_kind(external_pending|history_backlog)（第十九轮 P1-3 + 第二十轮 P1-2 补硬约束，external pending / history backlog 承接的普通 successor 因果键，+ `UNIQUE INDEX ON run_queue(continuation_from_execution_id) WHERE continuation_from_execution_id IS NOT NULL`（一父一子，镜像 superseded_from/migration_from_execution_id）+ 「三列至多一个非 NULL」CHECK 等价（中心事务+BEFORE 触发器）+ 「一父在三键并集下至多一后继」中心校验 + scope 一致(task/conversation/agent) + 创建后不可变 + 删除守卫 + kind↔父收尾路径匹配 + integrity scan;供全叶子聚合判 failed 父是否已被 pending successor 承接;`history_backlog_from_execution_id` 归一入此、不再单列;现网 `run_queue.source_run_id` 值引用 task_run id 是 attempt 溯源旧字段、与本 execution→execution 键维度不同不复用不改写;长期可选收敛为 execution_edges 边表）**/**process_cleanup_state(unconfirmed|confirmed)（第十六轮 P1-C，仅 null_conversation_migration 用：区分 NULL running 进程树是否确认退出，正交于 blocked_reason，避免用 process_not_confirmed_dead 作 reason 而误走 generic recovery）+ cleanup_version（第十七轮 P0，两阶段清理确认的 CAS 版本，unconfirmed→confirmed 翻转 +1）/cleanup_confirmed_at/cleanup_confirmed_by/cleanup_evidence_hash（第十七轮 P0，事务 B `confirm_null_process_cleanup()` 确认进程树退出的审计证据，attempt 保持 orphaned 不改写、清理确认落这三列而非 UPDATE task_runs.status）**/**terminal_source_status（第十七轮 P1-C，持久固化「进入终态时的 source status」∈{queued,claimed,running}不可变列，供 `final_attempt_id=NULL` 唯一例外的 DB CHECK 引用：仅 `recovery_blocked AND blocked_reason=null_conversation_migration AND terminal_source_status=queued` 时允许 final=NULL，写入后不可变;第十九轮 P1-4 + 第二十轮 P1-4：值域恒为 {queued,claimed,running} 三值不含 unknown;回填推断不了入独立 `terminal_source_backfill_quarantine` 隔离表`{execution_id, reason, detected_at, resolved_at, resolved_by}`、`terminal_source_status` 保持 NULL;activate 双条件门=quarantine 未解决=0 AND 终态 terminal_source_status IS NULL=0 才放行、fail-closed;并安装 `BEFORE INSERT/UPDATE` 触发器对 status/blocked_reason/final_attempt_id/terminal_source_status 跨列组合做写时守卫、终态 source 必为三值之一)**/**execution_attempt_retry_remaining + execution_attempt_retry_version（第十九轮 P1-1 + 第二十轮 P1-1 补 version/init/回填/lease reclaim：同一 execution 内 attempt 级重试预算，仅 transient/prestart/protocol-gate/**claimed lease reclaim** 回 `queued` 重试时扣减，扣减用 `remaining>0 AND version=expected` CAS + version+1，初始 `remaining=initial_retry_budget`(Paladin 配置)·`version=0`（根/continuation/recovery child 均此初始）、存量回填 initial_retry_budget、remaining=1 当次仍回队下次才终局、lease reclaim 计入同一预算防无限 crash-loop;与 `recovery_budgets.budget_remaining`（仅 supersede/reclaim/manual resume 生成 recovery child 才扣）严格分账、SHALL NOT 混用）**/rerun_requested/pending_through_message_id/**next_event_seq（第二十一轮 P0-1，per-execution 事件序号分配器：event writer 事务内锁 run_queue 行取 next_event_seq、写 event、+1，作 SSE 提交顺序安全游标基准，替代全局自增 id 作已提交水位）**;`task_runs` 加列 run_queue_id/attempt_no（`UNIQUE(run_queue_id, attempt_no)`，模型 A 一 execution 多 attempt）/worker_generation/worker_instance_id/pid_create_time/planned_through_msg_id/**failure_stage(prestart|running)**/**failure_class(infrastructure|configuration|business)**/**retryable**（第六轮 P1-2 方案 B 正交失败字段，取代独立 `prestart_failed` 状态）/**abandon_stage(prelaunch|running)**/**abandon_reason(lease_reclaim|null_conversation_migration|protocol_incompatible)**（第十六轮 P1-A：abandoned 扩义正交字段，定局性由 final_attempt_id 引用决定非 status;第十七轮 P1-B 补 protocol_incompatible 覆盖协议不兼容中止子类）;新增 `worker_state` 单行表（含 lease_expires_at/protocol_version）+ **`execution_events` 事件表（第二十一轮 P0-1 主键改为 `PRIMARY KEY(execution_id, event_seq)`、`event_seq` 由 run_queue 行锁分配的 per-execution 提交顺序安全序号，SHALL NOT 用全局自增 id 作续传游标——PG identity 在 INSERT 时分配不等提交、late-commit 小 id 会被 reader 永久越过）** + **`execution_edges` 统一边表（第二十一轮 P0-2，`{parent_execution_id, child_execution_id, edge_kind ∈ recovery|migration|external_pending|history_backlog}` + `UNIQUE(parent_execution_id)` + `UNIQUE(child_execution_id)`）——三类 successor helper 在同一事务先插 execution_edges 再建 child，单一 `UNIQUE(parent_execution_id)` 天然保证「一父至多一后继」跨键并集唯一，替代三分列 partial unique + `NOT EXISTS`（PG READ COMMITTED 下后者挡不住跨列并发双 child）;三个 predecessor 列仍保留作查询/审计，但并集唯一以边表为准** + **`recovery_operations` 表（第十九轮 P1-2 + 第二十轮 P1-3 改名收窄：**仅 recovery 分支 canonical operation/幂等、非 resolved 真相源、删 resolution_kind 列**，manual+reclaim 共用：`parent_execution_id PK/child_execution_id UNIQUE/operation_source(manual|reclaim)/canonical_payload_hash/created_at`）+ `recovery_requests` 表（第十四轮 P1-3 + 第十九轮 P1-2 收窄为仅人工 HTTP 请求幂等审计：`request_id PK/parent_execution_id/request_token/raw_payload_hash(仅审计,可选)/canonical_payload_hash(一切相等判断只比此字段)/outcome(winner|idempotent_replay|rejected_already_resolved)/resolved_child_id NULLABLE/actor/created_at` + `UNIQUE(parent_execution_id, request_token)`;自动 reclaim 无 token 只写 recovery_operations 不进此表）+ `recovery_budgets` 表（第十四轮 P1-3，恢复预算真相源：`recovery_chain_id(主键)/budget_limit/budget_remaining/version/last_reset_by/last_reset_at/last_reset_reason`，第十五轮 P1-5 字段名 budget_current→budget_remaining 消歧）+ `null_migration_requests` 表（第十五轮 P1-4，NULL conversation 迁移请求幂等：`parent_execution_id/request_token/raw_payload_hash(仅审计,可选)/child_execution_id/target_conversation_id/actor/created_at` + **`canonical_payload_hash`（一切相等判断只比此字段）+ `outcome(winner|idempotent_replay|rejected_already_resolved)`（第十七轮 P1-D，与 recovery_requests 同口径;第十九轮 P1-2 统一改用 canonical_payload_hash）** + `UNIQUE(parent_execution_id, request_token)`）**;**`run_queue.recovery_chain_id/recovery_attempt` 的根初始化（第十四轮 P1-3）：普通根 execution `recovery_chain_id=自身 execution id`、`recovery_attempt=0`;recovery child `recovery_chain_id=parent.recovery_chain_id`、`recovery_attempt=parent.recovery_attempt+1`;`superseded_from` 父存在性/自环/scope/attempt 单调按用户拍板走应用层 + 迁移探针 FK 等价校验（不重建表加原生 FK），仅自环可用列级 CHECK`(id != superseded_from)`；预算 CAS 与 child insert 同 `resume_blocked_execution()`/reclaim 事务）)、`routes/runs.py`(两段式 dispatch:POST 幂等入队+GET SSE 订阅、SSE 契约含 Last-Event-ID/superseded 跳转、kill request/ack)、`collab.py`(原子 claim CAS、`_running`→DB、`_loop` 挪 worker、触发合并不丢、reclaim 增强、交棒 generation/ack、fencing 校验、`finish_execution()` 统一收尾事务)、`executor/*.py`(进程 containment:Windows Job Object / POSIX PR_SET_PDEATHSIG+cgroup·systemd scope[非 `start_new_session` 冒充]、CAS 前启动闸门 Windows CREATE_SUSPENDED / POSIX pipe·eventfd gate、交棒杀 CLI)、新增 `worker.py` 入口 + supervisor、新增 `deploy/nginx.conf` + 蓝绿脚本、`jian` 写接口加 generation fencing 校验;**`tasks` 表加 `row_version INTEGER NOT NULL DEFAULT 0` 单调列（第二十轮 P1-5，migration API 预览快照 CAS 用，每次相关修改递增，替代用 updated_at 字符串做并发判据;第二十一轮 P1-3：所有 `UPDATE tasks` writer 均须递增、迁移用单语句原子 CAS `WHERE id=? AND row_version=?`）**;**`conversations` 表加 `next_message_seq INTEGER NOT NULL DEFAULT 0` + `messages` 加 `message_seq INTEGER`（第二十一轮 P0-1，per-conversation 提交顺序安全消息序号：消息 writer 事务内锁 conversations 行取 next_message_seq、写 message、+1，作 Session Resume 增量水位/migration watermark 基准，替代 `MAX(messages.id)` 全局 id——防 late-commit 小 id 用户消息被漏灌;`UNIQUE(conversation_id, message_seq)`）**;**新增 `terminal_source_backfill_quarantine(execution_id PK, reason, detected_at, resolved_at, resolved_by)` 隔离表（第二十轮 P1-4）**。**不涉及** detached 独立存活、文件唯一真相源、worker_id 认领、收尾重建（已砍）、tenant/space/membership ACL（第二十轮 P1-5：本 change 用现网 project_id 判同空间，细粒度成员 ACL 走独立授权 change）。
- **强依赖 [agent-session-resume]**：M2.5 的「resume 续跑」依赖该 change 先提供 per-agent `session_id` 存储与 `--resume` 能力。二者关系:agent-session-resume 是独立优化(省 token/上下文连贯),同时是本 change M2.5 的地基;本 change 只负责「重启时怎么中断、怎么重入队触发 resume」,不负责 resume 本身的实现。
- **与 [platform-concurrency-scaling] 强协调（关键）**：该 change 的阶段 2（2.1 调度状态外置 / 2.2 多 worker 无状态消费）与本 change 的 M2 是**同一块地基**。约定：**由本 change 的 M2 实现「调度状态外置 + worker 无状态化」,concurrency 的 2.1/2.2 复用该成果、不重复实现。** 本 change 聚焦「单 API + 单 Worker 的解耦与平滑重启」,concurrency 聚焦「多 worker 并发规模与公平」,二者共享外置地基、动机与验收各自独立。
  - 顺序建议：本 change M1(WAL)与 concurrency 阶段 0.1(WAL)是同一件事,谁先做另一个即已满足。
  - `_claim_one` **当前非原子**（SELECT + 无 CAS 的 UPDATE），本 change 阶段 1 补 CAS 后多进程消费地基才具备;concurrency 复用该成果（Review 修正原「已原子」误判）。
- **关联能力**：[agent-execution](执行/kill/孤儿兜底)、[agent-collaboration](调度/队列)、[task-system](run_logs/SSE)。
- **关联记忆**：`backend-restart-single-instance`(重启前须杀净 8100 监听、reload 双实例坑)——M3 的蓝绿正是这条坑的正式解法;落地后该手动流程被 Nginx reload 取代。
- **平台事实**：Windows 上 CLI 子进程用 `taskkill /F /T` 杀进程树;pid 带「创建时间指纹」防复用误杀——阶段 2 跨进程 kill 须沿用该指纹机制并在跨进程场景重新验证;并新增 Job Object containment 使 Worker 崩溃时 OS 自动清理。
